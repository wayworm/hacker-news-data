services:

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel run
    environment:
      TUNNEL_TOKEN: ${TUNNEL_TOKEN}
    networks:     
      - hn-network 
  db:
    image: postgres:14-alpine
    volumes:
      - /mnt/database/postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    networks:
      - hn-network
      
  web:
    build:
      context: .
      dockerfile: Project.Dockerfile
      target: analysis_image
    ports:
      - "5000:5000"
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_HOST=db 
      - POSTGRES_PORT=5432
    depends_on:
      - db
    networks:
      - hn-network
    volumes:
      - cache_data:/app/cache
      - image_data:/app/Analysis/time_series/static/images 

  scraper:
    build:
      context: .
      dockerfile: Project.Dockerfile
      target: scraper_image
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
    depends_on:
      - db
    networks:
      - hn-network

networks:
  hn-network:
    driver: bridge


volumes:
  cache_data:
  image_data: